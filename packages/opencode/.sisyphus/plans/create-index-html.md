# Create `index.html` with `Hello Auto Handover`

## TL;DR
> **Summary**: Add exactly one new file at `packages/opencode/index.html` as a minimal standalone HTML5 document whose only visible page text is `Hello Auto Handover`.
> **Deliverables**:
> - New file: `packages/opencode/index.html`
> - Agent-produced evidence proving pre-check failure, exact final content, and zero unrelated repo edits
> **Effort**: Quick
> **Parallel**: YES - 2 waves
> **Critical Path**: Task 1 → F1/F2/F3/F4

## Context
### Original Request
Create a simple `index.html` file whose content says `Hello Auto Handover`.

### Interview Summary
No interview was performed because automated mode required immediate planning. The plan uses the smallest complete interpretation that satisfies the request without adding serving, routing, styling, scripting, or metadata work.

### Metis Review (gaps addressed)
- Locked the ambiguity to a **minimal valid HTML document**, not a plain-text file with an `.html` extension.
- Added an explicit pre-check proving the task is initially unmet before implementation.
- Removed any need for browser/manual inspection; verification is deterministic and shell-executable.
- Hardened guardrails against path drift, unrelated file edits, and “helpful” webpage embellishment.

## Work Objectives
### Core Objective
Add `packages/opencode/index.html` with exact document structure and the exact visible body text `Hello Auto Handover`.

### Deliverables
- `packages/opencode/index.html`
- Evidence files under `.sisyphus/evidence/` generated by the executor during verification

### Definition of Done (verifiable conditions with commands)
- [ ] From repo root, `Test-Path "packages/opencode/index.html"` returns `True`
- [ ] From repo root, `(Get-Content "packages/opencode/index.html" -Raw) -replace "`r`n", "`n"` equals exactly `<!doctype html>`n<html lang="en">`n  <body>Hello Auto Handover</body>`n</html>`n`
- [ ] From repo root, `Select-String -Path "packages/opencode/index.html" -Pattern "<body>Hello Auto Handover</body>" -SimpleMatch` returns one match
- [ ] From repo root, `Select-String -Path "packages/opencode/index.html" -Pattern "<head>|<title>|<meta|<script|<style|<img|<link|<form|<div"` returns no matches
- [ ] From repo root, `git diff --name-only -- packages/opencode` outputs only `packages/opencode/index.html`

### Must Have
- Exactly one new file: `packages/opencode/index.html`
- The file contents must be exactly:

```html
<!doctype html>
<html lang="en">
  <body>Hello Auto Handover</body>
</html>
```

- The only visible page text is `Hello Auto Handover`
- The file must be UTF-8 text and may end with a single trailing newline
- No existing repo-tracked file may be modified

### Must NOT Have (guardrails, AI slop patterns, scope boundaries)
- No path other than `packages/opencode/index.html`
- No plain-text-only file contents; this must be valid minimal HTML
- No `<head>`, `<title>`, `<meta>`, `<script>`, `<style>`, `<img>`, `<link>`, `<form>`, `<div>`, comments, IDs, classes, or inline styles
- No routing, server, embedding, build, package, config, docs, or test changes
- No screenshots, browser validation, or manual review as acceptance evidence
- No additional files except executor-generated evidence artifacts under `.sisyphus/evidence/`

## Verification Strategy
> ZERO HUMAN INTERVENTION - all verification is agent-executed.
- Test decision: none + deterministic PowerShell checks from repo root
- QA policy: The single implementation task includes both pre-check and post-check scenarios; final review wave validates compliance, quality, QA, and scope
- Evidence: `.sisyphus/evidence/task-{N}-{slug}.{ext}`

## Execution Strategy
### Parallel Execution Waves
> Target: 5-8 tasks per wave. <3 per wave (except final) = under-splitting.
> This request is intentionally tiny, so Wave 1 contains the single atomic implementation task and Wave 2 is the mandatory parallel review wave.

Wave 1: Task 1 (`quick`) — add the single HTML file and prove exact content

Wave 2: Final verification wave — F1 (`oracle`), F2 (`unspecified-high`), F3 (`unspecified-high`), F4 (`deep`) in parallel

### Dependency Matrix (full, all tasks)
| Task | Depends On | Blocks |
|---|---|---|
| 1 | none | F1, F2, F3, F4 |
| F1 | 1 | completion |
| F2 | 1 | completion |
| F3 | 1 | completion |
| F4 | 1 | completion |

### Agent Dispatch Summary (wave → task count → categories)
- Wave 1 → 1 task → `quick`
- Wave 2 → 4 tasks → `oracle`, `unspecified-high`, `unspecified-high`, `deep`

## TODOs
> Implementation + Test = ONE task. Never separate.
> EVERY task MUST have: Agent Profile + Parallelization + QA Scenarios.

- [ ] 1. Add `packages/opencode/index.html`

  **What to do**: From repo root, first prove the file does not already exist, then create exactly one new file at `packages/opencode/index.html` with the exact bytes shown below, then run the post-check commands and save their outputs as evidence.

  ```html
  <!doctype html>
  <html lang="en">
    <body>Hello Auto Handover</body>
  </html>
  ```

  **Must NOT do**: Do not create the file anywhere else. Do not add a `<head>` block or any tags other than `<!doctype html>`, `<html lang="en">`, and `<body>Hello Auto Handover</body>`. Do not touch `src/server/routes/ui.ts`, `package.json`, tests, docs, or any other file.

  **Recommended Agent Profile**:
  - Category: `quick` - Reason: one-file deterministic change with no architectural decisions left to the executor
  - Skills: `[]` - no special skill is needed for a static HTML file addition
  - Omitted: `['playwright', 'git-master']` - no browser workflow or git operation is required for the implementation task

  **Parallelization**: Can Parallel: NO | Wave 1 | Blocks: F1, F2, F3, F4 | Blocked By: none

  **References** (executor has NO interview context - be exhaustive):
  - Pattern: `packages/app/index.html:1-23` - repo precedent for checked-in HTML entry files using `<!doctype html>` and `<html lang="en">`
  - Pattern: `packages/desktop/index.html:1-24` - confirms package-root `index.html` placement is an accepted repo shape in sibling packages
  - Pattern: `packages/desktop-electron/src/renderer/index.html:1-22` - further HTML document precedent, but this task intentionally uses a much smaller document
  - Context: `packages/opencode/src/server/routes/ui.ts:19-35` - package serves generated/embedded UI assets; do not modify this route or attempt to wire the new file into it
  - Context: `packages/opencode/package.json:8-18` - standard package scripts exist, but none require updates for this one-file addition

  **Acceptance Criteria** (agent-executable only):
  - [ ] Pre-check: `Test-Path "packages/opencode/index.html"` returns `False` before implementation
  - [ ] Post-check: `Test-Path "packages/opencode/index.html"` returns `True`
  - [ ] Post-check: `(Get-Content "packages/opencode/index.html" -Raw) -replace "`r`n", "`n"` equals exactly `<!doctype html>`n<html lang="en">`n  <body>Hello Auto Handover</body>`n</html>`n`
  - [ ] Post-check: `Select-String -Path "packages/opencode/index.html" -Pattern "<body>Hello Auto Handover</body>" -SimpleMatch` returns one match
  - [ ] Post-check: `Select-String -Path "packages/opencode/index.html" -Pattern "<head>|<title>|<meta|<script|<style|<img|<link|<form|<div"` returns no matches
  - [ ] Scope check: `git diff --name-only -- packages/opencode` outputs only `packages/opencode/index.html`

  **QA Scenarios** (MANDATORY - task incomplete without these):
  ```
  Scenario: Pre-check then happy path
    Tool: Bash
    Steps:
      1. Run `if (Test-Path "packages/opencode/index.html") { Write-Output "EXISTS" } else { Write-Output "MISSING" }` from repo root and save output.
      2. Create `packages/opencode/index.html` with the exact planned content and no other file changes.
      3. Run `if (Test-Path "packages/opencode/index.html") { Write-Output "EXISTS" } else { Write-Output "MISSING" }`.
      4. Run `$content = (Get-Content "packages/opencode/index.html" -Raw) -replace "`r`n", "`n"; if ($content -ceq "<!doctype html>`n<html lang=\"en\">`n  <body>Hello Auto Handover</body>`n</html>`n") { Write-Output "EXACT" } else { Write-Output "MISMATCH" }`.
      5. Run `Select-String -Path "packages/opencode/index.html" -Pattern "<body>Hello Auto Handover</body>" -SimpleMatch`.
      6. Run `Select-String -Path "packages/opencode/index.html" -Pattern "<head>|<title>|<meta|<script|<style|<img|<link|<form|<div"` and confirm zero matches.
      7. Run `git diff --name-only -- packages/opencode`.
    Expected:
      - Step 1 prints `MISSING` before implementation.
      - Step 3 prints `EXISTS` after implementation.
      - Step 4 prints `EXACT`.
      - Step 5 returns one match.
      - Step 6 returns no output.
      - Step 7 prints only `packages/opencode/index.html`.
    Evidence: .sisyphus/evidence/task-1-create-index-html.txt

  Scenario: Failure/edge case
    Tool: Bash
    Steps:
      1. Run `Select-String -Path "packages/opencode/index.html" -Pattern "Hello auto handover|Hello AutoHandover|Hello Auto Handover!"`.
      2. Run `Select-String -Path "packages/opencode/index.html" -Pattern "<head>|<title>|<meta|<script|<style|<img|<link|<form|<div"`.
      3. Run `git diff --name-only -- .` and inspect whether any path other than `packages/opencode/index.html` is listed as changed for this task.
    Expected:
      - Step 1 returns no matches.
      - Step 2 returns no matches.
      - Step 3 shows no unrelated implementation-file changes; only `packages/opencode/index.html` may appear for the task itself.
    Evidence: .sisyphus/evidence/task-1-create-index-html-error.txt
  ```

  **Commit**: NO | Message: `chore(opencode): add hello auto handover index page` | Files: `packages/opencode/index.html`

## Final Verification Wave (MANDATORY — after ALL implementation tasks)
> 4 review agents run in PARALLEL. ALL must APPROVE. Present consolidated results to user and get explicit "okay" before completing.
> **Do NOT auto-proceed after verification. Wait for user's explicit approval before marking work complete.**
> **Never mark F1-F4 as checked before getting user's okay.** Rejection or user feedback -> fix -> re-run -> present again -> wait for okay.
- [ ] F1. Plan Compliance Audit — oracle
- [ ] F2. Code Quality Review — unspecified-high
- [ ] F3. Real Manual QA — unspecified-high
- [ ] F4. Scope Fidelity Check — deep

## Commit Strategy
- Do not create a git commit unless the user explicitly asks for one.
- If a commit is later requested, create one atomic commit containing only `packages/opencode/index.html`.

## Success Criteria
- The repository contains a new file at `packages/opencode/index.html`.
- The file contents exactly match the planned minimal HTML document.
- The only visible body text is `Hello Auto Handover`.
- No other repo-tracked files are changed.
- Automated shell verification proves the task was initially unmet and is fully satisfied after implementation.
