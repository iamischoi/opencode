# OpenCode Internal Server Configuration
# ----------------------------------------------------
# 구동 환경에서 외부(opencode.ai) 서버 호출 없이 오직 지정된 
# 사내(Internal) AI 모델과 연동하기 위한 설정파일입니다.

# 1. 다중 커스텀 엔드포인트 설정 (Multi-Endpoint)
# 서로 다른 여러 개의 LLM 서버(공급자)를 등록할 때 사용합니다.
# 사용할 커스텀 공급자 이름 목록 (쉼표로 구분)
OPENCODE_CUSTOM_PROVIDERS=corp_main,corp_sub

# [첫 번째 공급자 (corp_main) 설정]
OPENCODE_PROVIDER_CORP_MAIN_URL=http://엔드포인트A.com/v1
OPENCODE_PROVIDER_CORP_MAIN_KEY=sk-1111
OPENCODE_PROVIDER_CORP_MAIN_MODELS=qwen-72b-coder,qwen-3-coder

# [두 번째 공급자 (corp_sub) 설정]
OPENCODE_PROVIDER_CORP_SUB_URL=http://엔드포인트B.com/v1
OPENCODE_PROVIDER_CORP_SUB_KEY=sk-2222
OPENCODE_PROVIDER_CORP_SUB_MODELS=gpt-4o,claude-3

# ----------------------------------------------------
# 2. 에이전트 및 모델 강제 라우팅 맵핑
# 외부(WebUI 등)에서 요청 시, 각 에이전트가 어떤 커스텀 에이전트 객체와 어떤 모델을 쓸지 지정합니다.
# 형식: 외부요청타입:플러그인에이전트명:공급자/모델명
OPENCODE_AGENT_MODELS=의뢰:oh_my_openagent_req:corp_main/qwen-72b-coder,검토:oh_my_openagent_rev:corp_sub/gpt-4o,개발:oh_my_openagent_dev:corp_main/qwen-3-coder,default:build:corp_main/qwen-3-coder

# ----------------------------------------------------
# 4. GitLab 연동 설정 (저장소 자동 클론)
GITLAB_HOST=https://gitlab.com
GITLAB_USERNAME=내계정아이디
GITLAB_TOKEN=발급받은PersonalAccessToken
GITLAB_AUTO_CLONE=false

# 5. 연동할 복수 저장소 목록 (파이프(|)로 구분)
# 형식: 리포지토리Path;클론될폴더명
# 위 OPENCODE_APP_CONFIG의 앱코드(S01)가 여기의 group명과 일치해야 합니다.
REPOSITORIES=S01/project-a.git;project-a|S02/project-b.git;project-b

# ----------------------------------------------------
# 6. 동적 모델(Agent) 생성 설정
# Open WebUI 등에 노출될 모델 목록을 설정합니다.
# 형식: 앱이름:앱코드 (쉼표로 구분. 앱코드는 GitLab Group명과 매칭되며 항상 3자리여야 함)
# 예: SOLT1:S01 -> S01은 GitLab의 Group명이자 앱의 고유 코드임
OPENCODE_APP_CONFIG=SOLT1:S01,SOLT2:S02

# 지원할 에이전트 종류 (쉼표로 구분)
OPENCODE_AGENT_TYPES=의뢰,검토,개발,검증
